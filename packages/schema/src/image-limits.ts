/**
 * Enforced by @jellyfindex/validate-content and fixed by @jellyfindex/optimize-images.
 */

/** Max width/height (px) for any raster preview image, logo, or banner. */
export const MAX_IMAGE_DIMENSION = 1920;

/** Max file size (bytes) for a raster (webp) image. */
export const MAX_RASTER_IMAGE_BYTES = 500 * 1024;

/** Max file size (bytes) for an SVG image. */
export const MAX_SVG_IMAGE_BYTES = 100 * 1024;
